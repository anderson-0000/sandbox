# Docker Desktop Kubernetes

このディレクトリには、Docker Desktop 上で動作する Kubernetes 環境に関する設定ファイルや各種リソース定義が格納されています。

## 概要

- `*.md`: Kubernetes の各機能やツールに関する説明ドキュメント
- `*.yml` / `*.yaml`: Kubernetes のリソース定義ファイル (Deployment, Service, CronJob など)

これらのファイルは、Docker Desktop 環境で Kubernetes クラスターを操作・設定する際に使用されます。
